// import renderText from './renderFunction.js';

// const infoButton = document.querySelector('#toggleInfo');
// infoButton.addEventListener('click', handleClick);

// let infoState = 0;
// function handleClick(event) {
//   if (infoState === 4) infoState = 0;

//   if (infoState === 3) {
//     renderText(120, 10, 'littleCanvas', 'Patrick Kaipainen   Web Developer,      Pixel enthusiast',[220,190,0], true);
//   }

//   if (infoState === 0) {
//     renderText(240, 10, 'littleCanvas', 'This is canvas destroyer. It pulls image data from two different images and combines it in a single array, which it draws to a canvas element. The images are configured to resolve at different widths.', [220,190,0], false);
//   }

//   if (infoState === 1) {
//     renderText(240, 10, 'littleCanvas', "Imagine taking every pixel from an image, and laying it out in a space that is only 1 pixel wide. You'd end up with a narrow 'picture' 1 pixel wide an n pixels long, where n would be the total number of pixels in the image.", [220,190,0], false);
//   }

//   if (infoState === 2) {
//     renderText(240, 10, 'littleCanvas', "Canvas destroyer draws the pixel data of an image into a space of any width from 1, to the original width of the image.  When the width of the space is the same as the width of the original image, the image resolves corectly.", [220,190,0], false);
//   }

//   if (infoState === 2) {
//     renderText(240, 10, 'littleCanvas', "But it also embeds a second image, which it configures to resolve at a different width.", [220,190,0], false);
//   }
//   infoState++;
// }


/*
Advance the user through the description with subsequent clicks of the infoButton



*/





